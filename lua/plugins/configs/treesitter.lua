local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "cpp", "c_sharp" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
