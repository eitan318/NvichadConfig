return {
  {
    "ThePrimeagen/vim-be-good", -- Plugin repository
    lazy = false, -- Load immediately (not lazily)
    cmd = { "VimBeGood" }, -- Optional: Only load when this command is called
    config = function()
      -- Additional configuration can go here if needed
    end,
  },
}
